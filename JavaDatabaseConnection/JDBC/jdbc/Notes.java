package jdbc;

public class Notes {
/*
 * we need a driver jar in order to integrate with the database.
 * we will have a separate driver jar for database
 * install mysql driver jar 
 * driver jar has to be imported to the package
 * download mysqlconnector.jar
 * 
 * how to import driver jar to the package:
 * right click on the project -> build path -> configure build path -> select class path
 * -> click on add external jar -> select mysql connector jar
 * 
 */
}
